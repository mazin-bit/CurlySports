# Force opening the app in your default browser when running npm start
# (Use ./scripts/open-url.js so the tab opens when Cursor/default opener doesn't)
BROWSER=./scripts/open-url.js

# Use port 3000 so the app URL is always http://localhost:3000
PORT=3000
